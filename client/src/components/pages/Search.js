import React from "react";
import Form from "./Form";
import Results from "./Results";

function Search() {
    return (
        <section>
            <Form />
            <Results />
        </section>
    );
}

export default Search;